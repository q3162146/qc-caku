# TTM 粘贴块：辨识光柱 + 场景名 + 采集段重测验证

> 用法：整段复制发给 TTM。代码已推 `github`（本机 `main` 最新 **`0a955d7`**），TTM 侧 `git fetch github && git rebase github/main` 构建真机。本块只列**验证项**；发现缺陷回传本仓。
> ⚠️ **本轮背景**：上两轮分别接入了 GameHUD 触屏摇杆+触摸视角（`c55662d` 本地核对通过），并给白模加了**桃花/守桃老人辨识光柱** + **场景名横幅**（`9ad228f`），解决「分不清哪个是桃花/守桃老人/当前场景」。本轮真机验证辨识度 + 触屏控制，并**自然走通** P02 走近老人 + P11 采花×5（上上轮因无移动控制被 `完成` 跳过）。
> 调试触屏按钮（左上角，S9 前移除）：行1 `S6链/断点/Spike`，行2 `完成/保存/读档`；`完成` 仅作卡住兜底，本轮 P02/P11 应能自然完成。

══════════════════════════════════════════════════════════

【辨识光柱 + 场景名 + 采集段重测验证】

## 一、同步与构建
- 同步：`git fetch github && git rebase github/main`（拉到 **`0a955d7`**；冲突回传勿硬解）。
- 官方构建真机（入口 `scripts/main.lua`；LSP 0 Error）。

## 二、A｜辨识度 + 触屏控制（核心）
1. ✅ **场景名横幅**：顶部居中显示 `「朝阳谷口」`（起始场景），随切换变 `「谷内桃林」` / `「洛水阴山」`。
2. ✅ **桃花光柱**：朝阳谷口应有 **5 根彩色光柱**（木绿/火红/土棕/金灰/水蓝）= 可采撷桃花；每朵球体上方一柱。
3. ✅ **守桃老人光柱**：朝阳谷口 **1 根金黄色较高光柱** = 走近交互的老人（`Int_oldman`）。
4. ✅ **触屏控制**：GameHUD 摇杆能移动/转向；右屏空白处滑动旋转相机；`Run`/`Jump` 按钮生效；**只有一个摇杆**（无双摇杆）。
5. ✅ **对话期锁移动**：打开任一对话/三选时角色不乱跑。
6. ✅ 全程**无 Lua error / 崩溃**。

> 若光柱看不到 / 横幅不显示 / 场景名不切：把启动日志（`[main] 检测到本地存档…`、`[Flow] 进入段落 …`、`[SceneManager]` 场景加载）回传。

## 三、B｜P02 走近守桃老人（重新自然走通）
1. 用摇杆走向**金黄色光柱那位的守桃老人**（`Int_oldman`）。
2. ✅ 走近触发**初见台词**（`[Flow] 走近交互 P02` + `远来的客人……`），播完 → 完成 P02 → 进 P03 开场三选。
3. 日志确认：`[PlayerController] 拾取标记: oldman`。

## 四、C｜P11 采花 ×5（重新自然走通）
1. 用摇杆走到每根**彩色光柱**（`Blossom_木火土金水`）处。
2. ✅ 每朵拾取弹**素女内心独白**（`[Flow] 拾取独白 <五行>`），进度 `[Flow] 桃花进度 N/5`。
3. ✅ 集齐 5 朵 → `桃花进度 5/5` → 完成 P11 → 进 P12。
4. 日志确认：`[PlayerController] 拾取标记: <五行>`、`[Flow] 拾取独白 <五行>`、`[Flow] 桃花进度 N/5`。

## 五、D｜顺带确认主链未受影响
- P03 开场三选 → P12 → ch2(无面鬼互动) → ch3(断点三选) → ch4(终局分叉) 应仍正常。时间充裕可快速走一遍；重点仍是 A/B/C。

## 六、回填要点（TTM 返回给我）
- 🔴 场景名横幅是否显示/随场景切换？桃花光柱 5 根 + 守桃老人光柱 1 根是否可见？
- 🔴 摇杆是否唯一能移动/转向？触摸视角、Run/Jump、对话期锁移动是否正常？
- 🔴 P02 是否走近老人触发初见台词并完成？P11 5 朵是否都触发独白、进度到 5/5？
- 全程 logcat / 录屏片段发我（尤其光柱、拾取独白、场景切换那几段）。

══════════════════════════════════════════════════════════
粘贴到此结束
══════════════════════════════════════════════════════════

---
## 给用户备忘（不要粘给 TTM）
- 本轮相关改动：`9ad228f`（桃花/守桃老人光柱 + 场景名横幅）+ `0a955d7`（doc）。
- ⚠️ 光柱为**白模辨识用**；光柱/横幅在正式素材到位后可作开发辅助（视需要保留或移除，标 S9 前）。
- 若某段仍卡住，`完成`(=F5) 可兜底，但本轮应能自然完成 P02/P11。
- 正式素材 S5/S7/S8/S9、S6-x 仍占位。
