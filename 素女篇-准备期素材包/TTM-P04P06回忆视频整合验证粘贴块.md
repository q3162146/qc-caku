# TTM 粘贴块：P04~P06 讲述段回忆视频整合验证

> 用法：整段复制发给 TTM。代码已推 `github`（本机 `main` 最新 **`568a0a6`**），TTM 侧 `git fetch github && git rebase github/main` 构建真机。本块只列**验证项**；发现缺陷回传本仓。
> ⚠️ **本轮背景**：P04~P06 是守桃老人讲述（`legend_part1/2/3`），原本 `dialogue` 段的 `video`(S2/S3/S4) 字段被忽略（MediaPlayer 只对 `type="video"` 段播）。本次接入：**先播回忆视频，播完再弹出该段讲述对白**。S2/S3/S4 为占位素材（沿用 `S1_test_mid`）。
> 调试触屏按钮（左上角，S9 前移除）：行1 `S6链/断点/Spike`，行2 `完成/保存/读档`；`完成`(=F5) 仅作卡住兜底。

══════════════════════════════════════════════════════════

【P04~P06 讲述段回忆视频整合验证】

## 一、同步与构建
- 同步：`git fetch github && git rebase github/main`（拉到 **`568a0a6`**；冲突回传勿硬解）。
- 官方构建真机（入口 `scripts/main.lua`；LSP 0 Error）。

## 二、验证步骤（新开一局：P01→P02→P03→P04）
1. 新档启动 → P01 开场视频 → P02 走近守桃老人 → P03 开场三选 → **P04**。
2. ✅ **P04 先播回忆视频**：日志 `[Flow] 讲述段 P04 先播回忆视频 S2` + `[Flow] 视频段落 S2 → MediaPlayer`，画面播 S2（占位 `S1_test_mid`）。
3. ✅ **视频播完弹讲述**：`[MediaPlayer] ENDED | 段落 P04 播放完成` → `[Flow] 对话段 P04（legend_part1）`，弹出守桃老人讲述文本（"很多年前，谷里住着一对药师夫妇……"），点「继续」推进 **P05**。
4. ✅ **P05 同理**（S3 → `legend_part2`：无幽听说洛水阴山藏圣药秘方……）；**P06 同理**（S4 → `legend_part3`：素女日日守在谷口……）。
5. ✅ 三段播完 → 推进到 **P07**（接任务：五朵桃花，`[Flow] 对话段 P07（quest_intro）`）。
6. ✅ 全程**无 Lua error / 崩溃**。

> 若回忆视频不播 / 播完不弹讲述 / 卡在 P04：把 `[Flow] 讲述段`/`[MediaPlayer]`/`[Flow] 对话段` 相关日志回传（尤其有无 `讲述段 P0x 先播回忆视频`）。

## 三、回填要点（TTM 返回给我）
- 🔴 P04/P05/P06 是否**先播回忆视频（S2/S3/S4）再弹讲述对白**？三段是否依次推进到 P07？
- 🔴 视频播完是否正常弹讲述、点「继续」是否推进？有无卡段/重复？
- 🔴 全程有无 Lua error / 崩溃？
- 全程 logcat / 录屏片段发我（尤其 P04 播视频→弹讲述、P05/P06 依次、P07 接任务那几段）。

══════════════════════════════════════════════════════════
粘贴到此结束
══════════════════════════════════════════════════════════

---
## 给用户备忘（不要粘给 TTM）
- 本轮改动：`568a0a6`（FlowController：`dialogue` 且带 `video` 段先播回忆视频，播完经 completeHandler 弹该段对白；新增 `pendingDialogueAfterVideo_`）。
- ⚠️ P04~P06 的讲述视频为占位（`S1_test_mid`）；正式 S2/S3/S4 到位后仅替换 `VIDEO_SOURCES` 映射。
- 若视频播完不弹讲述，优先查 `flow/FlowController.lua` 的 `completeHandler` 里 `pendingDialogueAfterVideo_` 是否被消费（播完即清）。
