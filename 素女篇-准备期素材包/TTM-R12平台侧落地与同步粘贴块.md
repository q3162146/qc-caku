# TTM 粘贴块：R12 镜头修复平台侧落地 + 真机复测 + 仓库同步（2026-08-22）

> **用法（这一节不要粘给 TTM）**
> - 本块 = 让 TTM 把已完成的 R12 修复**落地到平台侧仓库**并做**最后一次真机复测 + 仓库同步**，从而在平台侧也收官 S1。
> - 背景：R12 修复在**本地代码已全部完成并验证**（球体视觉缩小 + 相机参数 + 后墙外移等 6 处），`24` 表 R12=✅、CLAUDE.md=「R1~R12 全部 ✅ / S1 收官」；但**平台侧仓库尚未落地**该修复。本块目的 = 让两边一致。
> - 本地最终代码即「源真相」：`scripts/game/PlayerController.lua`、`scripts/game/SceneManager.lua`；对照《TTM-R12镜头修复交付.md》。

══════════════════════════════════════════════════════
以下为粘贴内容（整段复制）
══════════════════════════════════════════════════════

【任务｜R12 镜头修复平台侧落地 + 真机复测 + 仓库同步】

背景：《桃素洛无幽·素女篇》R12 镜头构图修复已在代码侧完成并验证（玩家球体视觉缩小 4 倍、相机参数调整、三场景后墙外移等 6 处），本地 `24-S1真机复盘检查表` 已标 R12=✅。但**平台侧仓库尚未落地**该修复。请让平台侧与本地对齐，收官 S1 真机复盘。

## 一、落地 6 处修改（以本地最终代码为准；《TTM-R12镜头修复交付.md》为对照）

1. `scripts/game/PlayerController.lua`
   - 视觉球体 scale `(0.6,0.9,0.6)@y0.95 → (0.45,0.6,0.45)@y0.65`（胶囊 0.7×1.8 仅保留碰撞）；
   - 相机 `distance 6.0→6.8`、`offset.y 2.0→2.3`、`fov 50→52`。
2. `scripts/game/SceneManager.lua`
   - 谷内桃林 `TaoA (4,0,16)→(7,0,15)`（近景靠边）；
   - 洛水阴山 `GhostStone x2→3.5`、`water 桃花随移 (2,0.6,16)→(3.5,0.6,16)`；
   - **三场景后墙外移（根因）**：朝阳 `halfDepth 25.5→32`、桃林/洛水 `22.5→29`（根因 = 边界墙离出生点太近致相机 distance 6.8 被墙压回 3.05m，单调调相机参数无效）。

## 二、官方构建 + 复测

- 官方构建（确保包含上述修改），LSP 0 error。
- **真机复测 R12**：三场景各截 9:16 图；确认玩家球体占屏 **1/6~1/10**、不遮挡无涕桃/前进路线；回填《素女篇-准备期素材包/24-S1真机复盘检查表.md》R12 = ✅。

## 三、仓库同步（两边一致）

- `git fetch origin && git rebase origin/main` 合并本地镜像；
- 确认 **CLAUDE.md / docs/memory-index.md / 24 表** 三处一致（本地均已标「R1~R12 全部 ✅ / S1 收官」，平台侧需与之对齐）；
- 若平台侧复测结果与本地 9.6%（球体占屏）一致，即可收官 S1；不一致贴出差异供比对。

## 四、注意事项

- 球体材质色 (242,224,199)≈朝阳雾色，**真机球体肉眼难辨**，需用「暖白像素量 + UI 在位 + 场景切换正常」等多证据间接确认修复生效（决策 D-012）。正式接入时角色/球体材质**务必用对比色**，避免「看不见主角」。
- 若本地与平台侧文档已因各自改动而分叉，请以本块 + 本地代码为准合并，勿互相覆盖。

══════════════════════════════════════════════════════
粘贴内容到此结束
══════════════════════════════════════════════════════

---

## 给用户（不要粘给 TTM）备忘

- 本块重点 = 让 TTM **落地 6 处修复到平台侧 → 构建 → 真机复测 R12 → 回填 24 表 → git 同步**（A）。
- 完成后，A 收官 S1；再进入 **B**：把《TTM-视频Spike浏览器验证粘贴块.md》发 TTM 做 Spike 的 WASM 预览验证。
- TTM 回贴后把「复测结果 + 同步结果」发回本会话，我据此复核并更新 CLAUDE.md/`24` 表。
