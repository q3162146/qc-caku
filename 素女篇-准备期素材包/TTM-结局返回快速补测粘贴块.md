# TTM 粘贴块：结局返回主菜单·快速补测（S6链直达）

> 用法：整段复制发给 TTM。代码已推 `github`（本机 `main` 最新 **`3eff7d3`**），TTM 侧 `git fetch github && git rebase github/main` 构建真机。本块只列**验证项**；发现缺陷回传本仓。
> ⚠️ **本轮背景**：正式主菜单已真机通过；**结局返回主菜单**（`S7/S8/S9` 视频播完 → `P99(end)` → 返回主菜单，不再循环 P01）代码侧已核对，但上两轮真机都停在 P11 采花、没走到结局。本块用左上角 `S6链`（=F10）**直达结局链**，快速验证「结局 → 返回主菜单」。

══════════════════════════════════════════════════════════

【结局返回主菜单·快速补测】

## 一、同步与构建
- 同步：`git fetch github && git rebase github/main`（拉到 **`3eff7d3`**；冲突回传勿硬解）。
- 官方构建真机（入口 `scripts/main.lua`；LSP 0 Error）。

## 二、快速路径（直达结局）
1. 启动 → 左上角**主菜单** → 点「开始游戏」进入 P01。
2. 点左上角 **`S6链`**（=F10）→ `[main] 触屏：直切 S6 记忆印证链（ch3/P31）` → P31 引导对话（点「继续」）。
3. P32~P36（S6-1..5）逐段：每段断点三选点一个 → **信念+1** → 续播 → 段间 `Destroy 存活=0`。
4. P36 完成 → P41 献花前 → P42 终局抉择（点「献上五朵桃花」）→ 结局视频（S7/S8/S9 占位）播完。
5. ✅ **关键**：应出现 **`[Flow] 结局段落（返回主菜单）`** + **主菜单再次弹出**（`[MainMenu] 已创建主菜单`/`Show`），且**不再循环回 P01**。
6. 返回后，主菜单「继续游戏」可读档续播（会续到 P99 end → 再弹主菜单，属当前设计；新一局请点「开始游戏」）。

## 三、回填要点（TTM 返回给我）
- 🔴 结局视频播完是否出现 `[Flow] 结局段落（返回主菜单）`？主菜单是否**再次弹出**（而非循环回 P01）？
- 🔴 返回主菜单后「开始游戏」是否全新开始，「继续游戏」是否可读档？
- 🔴 全程**无 Lua error / 崩溃**？
- 录屏片段/日志（尤其结局播完→主菜单再现那几帧）发我。

══════════════════════════════════════════════════════════
粘贴到此结束
══════════════════════════════════════════════════════════

---
## 给用户备忘（不要粘给 TTM）
- 本轮相关改动：`f2482ed`（主菜单/结局返回）+ `f47ecb2`（清档重置）+ `36b2591`（TTM 补丁）+ `3eff7d3`（doc）。
- ⚠️ 结局视频 S7/S8/S9 为占位素材；正式结局画面/制作名单后续替换。
- 若 `S6链` 直切后 P31 进入失败，检查 `FlowController.DebugJumpToParagraph("P31")` 是否被主菜单初始化前调用（应在 `FlowController.Init` 之后可用）。
