# TTM 粘贴块：S1/R12 + 视频 Spike 收尾清单（2026-08-23）

> **用法（这一节不要粘给 TTM）**
> - 本块 = 收尾清单，整段复制发给 TTM。其中「真机补测后台/前台」为真机项（用户侧执行），其余多为平台侧/流程项。
> - 当前状态：① R12 修复已落地平台侧并复测 ✅（仅剩 git 同步）；② 视频 Spike 已 WASM 验证全通过 ✅。以下为正式收官前剩余项。

══════════════════════════════════════════════════════
以下为粘贴内容（整段复制）
══════════════════════════════════════════════════════

【收尾清单｜S1/R12 + 视频生命周期 Spike 之后的剩余项】

背景：《桃素洛无幽·素女篇》已完成 R12 镜头修复（平台侧已落地 + 复测 R12=✅，S1 真机复盘 R1~R12 全部 ✅）与视频生命周期 Spike（WASM 预览真播放验证全通过，《22》§2 规格成立、S6 可按此接入《21》§5 13 段断点视频）。以下为收官剩余项。

## 1. R12 仓库同步（A 唯一遗留）
- 平台侧 git 本地**无 origin 远程**：R12 相关修改（代码+`24`表+文档）已提交到本地 master `3345689`。
- 请：配置正确远程（origin）后执行 `git fetch origin && git rebase origin/main`；或将提交 `3345689` 同步到平台仓库。
- 并确保 **CLAUDE.md / docs/memory-index.md / 24-S1真机复盘检查表.md** 三处一致（均已标「S1 真机复盘收官 / R1~R12 全部 ✅」），平台侧对齐后 **S1 正式收官**。

## 2. 真机补测「视频后台/前台」行为（B 遗留，真机项）
- Spike 已订阅 `AppDidEnterBackground / AppDidEnterForebackground` 并在后台自动 Pause/记录断点，但本次 WASM 未实测。请在真机补测：
  - 视频播放中**切后台**再回前台，记录：视频是否自动暂停？断点（mediaPos.timeSec）是否记录？回前台是否按断点恢复/可续播？
  - 顺带确认：通知栏下拉返回、多播放器内存、音画同步（S1 首次真机必测项）。
- 回填到《素女篇-准备期素材包/TTM-视频Spike结果汇报.md》§六 遗留 ①。

## 3. 正式 media 模块接入要点（供后续 S6 落地，非本次实现）
- 按《22》§2 规格（H.264/yuv420p/1080×1920/30fps/AAC-LC 48kHz）；Spike 已验证可行。
- 播放器数量从严：**同屏 ≤2**（剧情播放器 1 + 循环背景 ≤1；Spike 显示 3 实例虽可用但为省资源建议按 ≤2）。
- 断点恢复：写 `PlayerData.mediaPos { node, video, breakpoint, timeSec }`，播放/暂停时更新；恢复时按状态机 `CREATING→WAIT_READY→REQUEST_SEEK→WAIT_SEEK_CONFIRM→PAUSED_AT_TARGET→REVEAL` 对齐。
- 参考：保留 `scripts/experiments/VideoSpike.lua` 作参考，或按 S9 纪律移除；核心契约沉淀进正式 media 模块。

## 4. S9 发布前必须移除的调试代码（提醒，非本次）
- Spike：`F6` 触发 + 右上角「Spike」按钮 + `scripts/experiments/VideoSpike.lua`。
- 三指切场景调试手势（`main.lua`）。
- 未来顶部 HUD 若加入，需重新评估 SafeAreaView `nativeMenuInset=true`。

## 5. 两工作区一致性（重要）
- 素材路径：平台侧 `video/短视频生命周期 spike（推荐）/`（**全角括号**），本地曾有 `assets/Videos/`——正式接入须固定资源根路径，避免混淆。
- 驱动名：Spike 现暴露 `Update(dt)` 与 `Process(dt)` 别名；`main.lua` 用哪个都可以，保持一致即可。
- `docs/memory-index.md` / CLAUDE.md 两边各自更新过，务必在 git 同步时一次性对齐，避免分叉。

══════════════════════════════════════════════════════
粘贴内容到此结束
══════════════════════════════════════════════════════

---

## 给用户（不要粘给 TTM）备忘

- 第 1 项（git 同步）是 R12/S1 收官的**唯一硬遗留**，TTM 完成即收官 S1。
- 第 2 项（真机补测后台/前台）需要你在真机跑，还涉及多播放器内存/音画同步（S1 首次真机必测）。TTM 若能提供 adb 抓包清单更好。
- 第 3/4/5 项多为后续 S6/正式接入及流程提醒，不阻塞当前收官。
