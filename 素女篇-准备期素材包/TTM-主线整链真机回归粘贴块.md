# TTM 粘贴块：主线整链真机回归（P01→ch1→ch2→ch3→ch4→结局）

> 用法：整段复制发给 TTM。代码已推 `github`（本机 `main` 最新 **`f708e58`**），TTM 侧 `git fetch github && git rebase github/main` 后构建真机/WASM。正式视频 S5/S7/S8/S9 与 S6-x 仍为占位（沿用 `S1_test_mid`），S6 断点 `at=4.0` 为占位值。本块只列**验证项**；发现缺陷回传本仓。
> ⚠️ **调试用触屏按钮（左上角，S9 前移除）**：行1 `S6链/断点/Spike`，行2 `完成/保存/读档`。手机 F1~F12 按不了，调试一律用触屏按钮。

══════════════════════════════════════════════════════════

【主线整链真机回归｜新增 ch2 洛水阴 + P11 桃花独白 + ch4 终局】

背景：主线结构已全部打通（ch0 楔子→ch1 收集→ch2 洛水阴→ch3 六艺记忆印证→ch4 终局→结局），本轮**首次真机走完整条链**，重点覆盖本轮新增：P11 拾取独白、ch2 无面鬼互动、ch4 终局按信念轴分叉。

## 一、同步与启动
- 同步：`git fetch github && git rebase github/main`（拉到 **`f708e58`**；冲突回传勿硬解）。
- 建议先清存档（`saves/slot1.json`）验证**全新启动**；不清则顺带验证**自动续档**。

## 二、主链走查（逐段核对日志）

### A. ch0 楔子 + ch1 收集
1. 启动 → P01(S1) 视频播完 → P02 走近守桃老人（`Int_oldman`）触发初见台词 → P03 开场三选（不计信念，记录 `flags.open_choice`）→ P04~P06 讲述 → P07 接任务（五朵桃花）→ **P11 收集 5 朵**。
2. **P11 重点**：满场找 5 个 `Blossom_*`（木火土金水）逐一拾取，每朵**弹素女内心独白**（日志 `[Flow] 拾取独白 <key>`），进度 `[Flow] 桃花进度 N/5`；集齐 5 → `桃花进度 5/5` → 进入 P12。
3. P12 前往洛水阴山 → 进入 **ch2**。

### B. ch2 洛水阴（新增）
4. P21(S5 无面鬼初见视频, scene=洛水阴山) 播完 → P22 **无面鬼互动三选**（递上一碗山泉 / 默默陪他坐着 / 轻声唤：无幽）→ 选中即弹**反馈独白** → 进入 ch3/P31。
5. 三选**不计信念**（只记录 `flags.noface_action`）。

### C. ch3 记忆印证（打通到 ch4）
6. P31 引导 → P32~P36(S6-1..5) 五段，每段 `at=4.0` 断点**三选** → `显示三选` → 选中即锁 → **信念+1** → 续播 → 段间 `Destroy 存活=0` → P36 完成 → 进入 **ch4**。

### D. ch4 终局（新增）
7. P41 献花前（守桃老人, scene=朝阳谷）→ P42 **终局抉择**（献上五朵桃花 / 转身离开）→ 按**最高信念轴**出结局：重逢→S7圆满 / 放手→S8放手 / 传说→S9传说 → 结局视频播完 → P99（演示闭环回 P01）。
8. 信念轴预期：5 段记忆印证各 +1，总选决定最高轴（如五段全选「重逢」→ `reunion=5` → S7 圆满）。

## 三、回填要点（TTM 返回给我，我核对）
- 🔴 每段是否按上述走通？有无 Lua error / 崩溃（尤其 P11 独白弹窗、ch2 无面鬼互动、ch4 终局分支）。
- P11：5 朵是否都弹独白？进度是否 `5/5`？
- ch2：无面鬼互动三选是否正常弹/选/出反馈？
- ch4：终局抉择后是否按最高信念轴出对应结局（S7/S8/S9）？
- 全程 logcat / 录屏片段发我。

══════════════════════════════════════════════════════════
粘贴到此结束
══════════════════════════════════════════════════════════

---
## 给用户备忘（不要粘给 TTM）
- 本块 = 主线整链首次真机回归；本轮相关改动：`5943f2c`(ch2/ch4) + `060a8d8`(P11 桃花独白) + `f708e58`(ch4 献花前/终局回朝阳谷场景)。
- ⚠️ **真机采集段尚缺移动控制**：手机无法移动/转视角（`PlayerController` 只读 WASD/鼠标，没接屏上摇杆）→ P02 走近老人 / P11 采花**无法自然完成**，真机需先用左上角 `完成` 按钮跳过；需集成 `GameHUD` 触屏摇杆+触摸视角后方可真机验证采集段。
- ⚠️ P04~P06 的 `video`(S2/S3/S4) 是 `dialogue` 段字段，MediaPlayer 仅对 `type="video"` 段落做播放；若回忆视频未播放，属**既有**对话+视频整合缺口（非本次范围），可后续接。
- S5/S7/S8/S9、S6-x 均为占位素材；独白五行↔地点映射为占位。
