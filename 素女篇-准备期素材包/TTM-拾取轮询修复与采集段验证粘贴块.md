# TTM 粘贴块：拾取轮询修复 + 触屏 + 辨识度（采集段重测）

> 用法：整段复制发给 TTM。代码已推 `github`（本机 `main` 最新 **`177170e`**），TTM 侧 `git fetch github && git rebase github/main` 构建真机。本块只列**验证项**；发现缺陷回传本仓。
> ⚠️ **本轮背景**：前几轮已接入 GameHUD 触屏摇杆+触摸视角（`c55662d`）与白模辨识（桃花/守桃老人光柱 + 场景名横幅，`9ad228f`），但真机拾取仍失败——根因是拾取依赖 `PhysicsCollisionStart`，而 **KinematicCharacterController 不触发该事件**。本轮已改为 **`Update` 内距离轮询**（`5e2ea8f`）并**稳健化**（`177170e`：标记/光柱改由 FlowController 确认采集后移除，对话中不消费），再**加边沿触发 + 仅 explore 启用**（`fccfc8b`：进入半径才上报、非 explore 不刷屏），并给独白缺失加 **p 上下文兜底日志**（定位 wood 独白未显示）。应使「走近拾取/交互」真正生效且每朵都弹独白、无重复、不刷屏。本轮**重点重测 P02 走近老人、P11 采花×5**（前几轮都被 `完成` 跳过）。
> 调试触屏按钮（左上角，S9 前移除）：行1 `S6链/断点/Spike`，行2 `完成/保存/读档`；`完成` 仅作兜底，本轮 P02/P11 应能自然完成。

══════════════════════════════════════════════════════════

【拾取轮询修复 + 触屏 + 辨识度｜采集段重测】

## 一、同步与构建
- 同步：`git fetch github && git rebase github/main`（拉到 **`177170e`**；冲突回传勿硬解）。
- 官方构建真机（入口 `scripts/main.lua`；LSP 0 Error）。

## 二、A｜触屏 + 辨识度（快速确认）
1. ✅ **GameHUD 摇杆**：仅一个，拖动角色能移动/转向；右屏空白滑动旋转相机；`Run`/`Jump` 生效；对话期角色不乱跑。
2. ✅ **场景名横幅**：顶部 `「朝阳谷口」`，随切换变 `「谷内桃林」/「洛水阴山」`。
3. ✅ **光柱**：桃花 5 根彩色光柱（木绿/火红/土棕/金灰/水蓝）+ 守桃老人 1 根金黄光柱；采完桃花光柱应随之消失（不残留空柱）。

## 三、B｜P02 走近守桃老人（重点：轮询拾取）
1. 用摇杆走向**金黄光柱的守桃老人**（`Int_oldman`）。
2. ✅ 现在**走近即应触发**`[PlayerController] 拾取标记: oldman` → `[Flow] 走近交互 P02` → 初见台词（`远来的客人……`）→ 完成 P02 → 进 P03 开场三选。
3. 若仍不触发：把 `拾取标记`/`走近交互` 相关日志回传（尤其是否有 `[PlayerController]` 行）。

## 四、C｜P11 采花 ×5（重点：轮询拾取）
1. 用摇杆走到每根**彩色光柱**（`Blossom_木火土金水`）。
2. ✅ 每朵**走近即拾取**：`[PlayerController] 拾取标记: <五行>` → 弹素女内心独白（`[Flow] 拾取独白 <五行>`）→ `[Flow] 桃花进度 N/5`。
3. 🔴 **每朵都弹独白**（木/火/土/金/水 5 朵，wood 也应见 `拾取独白 wood`）；若某朵仍无独白，把 `无独白映射` / `独白数据缺失` 日志（含 `p=` / `type=`）回传。
4. 🔴 **无重复** `拾取标记`（同一五行不重复出现）；采集后**该光柱消失**（不残留空柱）。
5. ✅ 集齐 5 朵 → 完成 P11 → 进 P12。

## 五、D｜顺带确认主链未受影响
- P03 开场三选 → P12 → ch2(无面鬼互动) → ch3(断点三选) → ch4(终局分叉) 应仍正常。时间充裕可快速走一遍；重点仍是 B/C。

## 六、回填要点（TTM 返回给我）
- 🔴 P02 走近老人是否**首次自然触发** `拾取标记: oldman` + 走近交互并完成？
- 🔴 P11 5 朵是否**走近即拾取**、**每朵都弹独白（含 wood）**、进度到 `5/5`、光柱随采消失、无重复 `拾取标记`？
- 🔴 摇杆是否唯一可移动/转向？触摸视角、Run/Jump、对话期锁移动、场景名横幅、光柱是否正常？
- 全程 logcat / 录屏片段发我（尤其走近老人、采花拾取、光柱消失那几段）。

══════════════════════════════════════════════════════════
粘贴到此结束
══════════════════════════════════════════════════════════

---
## 给用户备忘（不要粘给 TTM）
- 本轮关键改动：`5e2ea8f`（拾取改距离轮询——修复真机 KCC 下 PhysicsCollisionStart 不触发导致拾取失败）+ `177170e`（稳健化：标记/光柱改由 FlowController 确认采集后移除，对话中不消费）。此前：`9ad228f`（光柱+场景名横幅）、`c55662d`（GameHUD 触屏）。
- ⚠️ 若本轮 P02/P11 仍不拾取：优先查 `PlayerController.Update` 的轮询是否执行（`blossomHandler_`/`scene_` 是否就绪）、`PICKUP_RADIUS=1.1` 是否够、光柱是否被误当作标记。
- 光柱/横幅为白模辨识用；正式素材到位后可作开发辅助（标 S9 前）。
- 正式素材 S5/S7/S8/S9、S6-x 仍占位。
