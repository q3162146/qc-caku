# TTM 粘贴块：S6 剩余验证回归（读档恢复 + 连续 5 段）

> 用法：整段复制发给 TTM。代码已由本仓合入并推 `github`（本机 `main` 最新），TTM 侧 `git fetch github && git rebase github/main` 拉到后再构建真机/WASM。测试视频仍为占位，请勿当作正式素材。
> 本块只列**验证项**；如发现代码缺陷，回传给本仓（或直接 TTM `git push github main`，PAT 已在 TTM 环境）。

══════════════════════════════════════════════════════════

【S6 剩余验证回归｜读档恢复 + 连续 5 段】

背景：代码已就绪（v0.1.7-S6），上轮已真机验证过断点 Hook / 冻结自愈 / 断点不重复 / 强制 ENDED / Destroy=0。本轮补两条链：
A) **完整读档恢复**（磁盘存档 slot1 + 启动自动续档）
B) **S6 记忆印证连续 5 段**（ch3/P32~P36，断点三选 + 信念+1）

## 一、构建前注意
- 同步：`git fetch github && git rebase github/main`（本机 `main` 含 `59b667e`(P02) + `v0.1.7-S6` 提交；确保拉到最新）。
- 测试视频路径：`video/短视频生命周期 spike（推荐）/S1_test_mid_6Mbps.mp4`（全角括号，官方构建已含）。S6-x 现占位同文件。
- 本块不涉及广告/分享卡。

## 二、A｜完整读档恢复（回归路径）
1. 启动 → P01 开场视频 → 播完自动进 P02（走近守桃老人，触发初见台词）→ P03 开场三选。
2. 用 **F8** 存一次档（写 `saves/slot1.json`；日志 `[PlayerData] 已保存到 saves/slot1.json`）。
3. 退出游戏 → 重新启动。
4. ✅ 期望：启动日志出现 `检测到本地存档，尝试自动续档` + `[Flow] 读档恢复：定位段落 …`，且从上次段落的**正确位置/进度**继续（含 belief/桃花等状态保留）。
   - 若上次在视频中段存档：重启后 `mediaPos.node` 命中 → 视频应从命中时间续播（`READY → SEEK_READ` → Seek 双确认 → 移除遮罩），**不闪第 0 帧**，**断点不重复触发**。
5. 边界：无存档时，启动应全新走 P01；日志 `[PlayerData] 无本地存档 …` + `[Flow] 流程启动（白模演示）`。
6. **F9** 手动读档续播：`[main] 调试：从 slot1 读档并续播` → 同上逻辑（无存档则提示失败）。
7. ⚠️ WASM 平台 savedata 存内存文件系统，刷新即丢（读档恢复需**真机**验证持久化）。

## 三、B｜S6 记忆印证连续 5 段（回归路径）
1. 启动后按 **F10**：`[main] 调试：直切 S6 记忆印证链（ch3/P31）` → 进入 ch3/P31 引导对话（2 行台词）。
2. 引导播完 → P32(S6-1) 视频 → `at=4.0` 断点暂停 → **解读三选**弹三项（信念+1，日志 `断点交互选择已锁定 | key=… | belief=…`）→ 选完锁定 → 继续播放 → 播完自动进 P33。
3. 依次走完 P33/P34/P35/P36（S6-2~S6-5），观察：
   - ✅ 每段：三选正常弹出、选中即锁、信念正确 +1（读 belief 日志）。
   - ✅ **5 段连播无无声 / 无重复播放 / 无上一段延迟回调串台**（《21》§7 验收 #3）。
   - ✅ 段间播放器释放：`Destroy 存活=0`，同屏剧情播放器=1（《21》§6.4）。
   - ✅ 播完 P36 后：`[Flow] 找不到下一段 P41`（ch4 未建，属预期收尾提示，非缺陷）。
4. 每段 `at=4.0` 为占位值（正式 S6-x 素材到位后按《21》§5 真机校正；占位测试视频时长 10s）。

## 四、回填要点（TTM 返回给我，我核对并回填 memory-index）
- A 项：启动续档是否从正确位置继续？是否不闪第 0 帧、断点不重复？belief/桃花是否保留？无存档是否全新开始？
- B 项：5 段是否连播无串台/无重复/无上段回调？每段三选+信念+1 是否正常？播放器是否归零？
- 游戏进程是否有 Lua error / 崩溃？logcat / 录屏片段文件发我需要。

══════════════════════════════════════════════════════════
粘贴到此结束
══════════════════════════════════════════════════════════

---
## 给用户备忘（不要粘给 TTM）
- 本块 = 验证驱动，无新代码；代码改动见 v0.1.7-S6（PlayerData Save/Load + FlowController Resume/自动存档 + main F8/F9 + ch3 + MediaPlayer 断点三选泛化 + F10）。
- ⚠️ 除上两条外，S6 断点 Hook/冻结自愈等上轮已验证项无需重复；如 TTM 顺带复测更佳。
- 若 TTM 反馈"读档恢复在真机不持久"，优先查：是否真机（WASM 会丢）；`saves/` 目录隔离是否正常。
