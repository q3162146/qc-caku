# TTM 粘贴块：正式主菜单 + 结局返回验证

> 用法：整段复制发给 TTM。代码已推 `github`（本机 `main` 最新 **`f47ecb2`**），TTM 侧 `git fetch github && git rebase github/main` 构建真机。本块只列**验证项**；发现缺陷回传本仓。
> ⚠️ **本轮背景**：先前启动即自动续档；现改为**正式主菜单**（标题 + 开始游戏/继续游戏，玩家选择）+ **结局返回主菜单**（结局视频 S7/S8/S9 播完 → 结局段 → 回主菜单，不再 P99→P01 演示循环）。另「开始游戏」**清档重置**（删 `saves/slot1.json` + 全新数据）。

══════════════════════════════════════════════════════════

【正式主菜单 + 结局返回验证】

## 一、同步与构建
- 同步：`git fetch github && git rebase github/main`（拉到 **`f47ecb2`**；冲突回传勿硬解）。
- 官方构建真机（入口 `scripts/main.lua`；LSP 0 Error）。

## 二、A｜主菜单
1. ✅ **启动显示主菜单**（不再自动续档）：标题「桃素洛无幽 · 素女篇」+ 副标题 + 按钮。
2. ✅ **开始游戏**：点击 → 日志 `[PlayerData] 已删除存档 saves/slot1.json`（若之前有档）→ 进入 **P01 开场视频**，全新开始。
3. ✅ **继续游戏**：
   - **有存档**：按钮可点 → 读档续播（`[Flow] 读档恢复：定位段落 …`）。
   - **无存档**：按钮灰置/不显示。
4. ✅ **菜单锁移动**：主菜单打开时角色不动（摇杆无效）。
5. ✅ 全程**无 Lua error / 崩溃**。

## 三、B｜结局返回主菜单
1. 走到结局（或调试直切）：结局视频 S7/S8/S9 播完 → 日志 `[Flow] 结局段落（返回主菜单）` + **返回主菜单**（不再循环回 P01）。
2. 返回后主菜单的「继续游戏」应仍可读档续播。

## 四、C｜开始新游戏清档（重点核对）
1. 先在某段存档（菜单/调试 `保存`）。
2. 返回主菜单 → 点「**开始游戏**」→ 日志 `[PlayerData] 已删除存档 saves/slot1.json` → 全新从 P01 开始。
3. 确认此时 belief/桃花为**全新默认值**（不再沿用上一局累加）。

## 五、回填要点（TTM 返回给我）
- 🔴 主菜单是否显示？「开始游戏」是否全新开始（P01）？「继续游戏」是否有档才可点/可续播？
- 🔴 结局视频播完是否**返回主菜单**（而非循环回 P01）？返回后「继续游戏」是否正常？
- 🔴 「开始游戏」是否清档（删除 slot1日志）+ belief/桃花重置？菜单打开时是否锁移动？
- 🔴 全程有无 Lua error / 崩溃？
- 全程 logcat / 录屏片段发我（尤其主菜单、开始/继续、结局返回、清档那几段）。

══════════════════════════════════════════════════════════
粘贴到此结束
══════════════════════════════════════════════════════════

---
## 给用户备忘（不要粘给 TTM）
- 本轮改动：`f2482ed`（主菜单/结局返回）+ `f47ecb2`（开始新游戏清档重置 `PlayerData.Clear`）。
- ⚠️ 存档/读档菜单(SaveMenu)与主菜单并存，均可存取档；S9 前合并入正式系统。
- 「继续游戏」无存档时按钮灰置（IsVisible/IsDisabled）；若有存档但读档失败会提示。
- 结局视频 S7/S8/S9 为占位素材；正式结局画面/制作名单后续替换。
