# TapTap 制造（TTM）开工提示词包

> 依据：TapTap 制造支持上传自定义游戏文档和素材，AI 按文档研究后开发（UrhoX + Lua）。
> 用法：8/19 新建参赛项目并绑定活动后，将本文档与设计文档一起上传/粘贴，按会话逐个开工。

---

## 0. 开工前必做（8/19 当天）

1. 报名（需提前完成并收到站内信确认）。
2. 在 TapTap 制造**新建参赛项目**（8/19 后）。
3. 用该项目**绑定本期 Game Jam 活动**，领取活动专用 100,000 Token 积分。
4. 上传设计文档包：设计总览、剧本与对白、记忆印证、文化方案、札记文案。
5. 在项目内用活动积分**重新生成/校验四张定妆图**（提示词见《11-美术设定参考》），确认后作为 Seedance 图生视频底图。
6. 将以下提示词粘贴给 TTM 的 AI 开发 Agent（嗒啦啦）。

> ⚠️ 绑定后不可改绑、不可取消；活动积分仅限该绑定项目使用，且优先消耗。

---

## 1. 项目初始化提示词（第一会话，可整段粘贴）

> 请先阅读我上传的设计文档，然后为我新建一个单机叙事游戏项目《桃素洛无幽·素女篇》。这是 TapTap 制造 × Seedance 主题赛的参赛作品，核心要求如下：
>
> 1. 遵守引擎规范：scripts/main.lua 为唯一业务入口；启动时读取 .project/settings.json，校验 multiplayer.enabled 为 false（单机模式），异常时打日志退出；模块按功能拆分，单文件不超过 1500 行；资源路径从资源根下一级开始，使用 cache:GetResource，禁止本地绝对路径。
> 2. 常规 UI（对话、选项、字幕、任务提示、札记）统一使用 urhox-libs/UI；自定义特效（桃花粒子等）用 raw NanoVG；不混用。
> 3. 输入使用 InputManager 抽象层，事件必须用 KEY_*/MOUSEB_* 枚举，禁止数字常量。
> 4. 世界单位用米，Y 轴向上；角色高度约 1.6~1.8 米；模型尺寸用 boundingBox 动态获取。
> 5. 规则与表现分离：输入 → 规则判断 → 数据变化 → 表现；演出/动画/音频函数内禁止修改游戏数据。
> 6. 数据安全：PlayerData/GameData 提前定义固定字段，存档加载做类型检查与默认值兜底。
> 7. 日志用 print("[模块名] 消息")。
>
> 本会话只做：创建项目骨架、三个场景的白模（朝阳谷口/谷内桃林/洛水阴山）、玩家移动与相机、基础碰撞。完成后列出下一步建议，不要继续实现其他系统。

---

## 2. 会话拆分清单（每个会话一个独立任务，避免上下文漂移）

| 会话 | 任务 | 输入文档 | 产出 |
|------|------|----------|------|
| S1 | 项目骨架 + 三场景 + 移动相机 | 设计总览 + 《13-屏幕方向与UI适配方案》（三场景白模需按 9:16 相机比例） | 可行走 demo |
| S2 | 对话系统 + 守桃老人传说（六段） | 02 剧本 | 可对话的 NPC |
| S3 | 探索与收集：五朵桃花（五行）+ 旅人札记图鉴 | 06/08 | 收集闭环 |
| S4 | 洛水阴山 + 无面鬼互动（三选项 + 信念值） | 02/05 | 抉择数据落库 |
| S5 | 记忆印证系统（5 段 + 解读选项 + 信念结算）；先以占位图/静态图跑通逻辑，S6 接入真实视频后替换 | 05 | 试炼闭环 |
| S6 | Seedance 视频播放接入 + 字幕 + 口型片段 | 01/10 | 演出接入 |
| S7 | 终局抉择（献花/摘桃两行动）+ 三结局判定 + 结局分享卡 | 05/07 | 完整通关 |
| S8 | UI 打磨（章回体卡/印章/题跋/标题收集 0/3）+ 存档/读档系统 + 标题界面（继续/新游戏/故事回看）+ 设置界面（字幕开关、音乐/音效/配音音量三档）+ 演出跳过（首次不可跳、二周目可跳） | 06/11 | 视觉统一 |
| S9 | 配音/音乐/音效接入 + 打磨测试 + 三结局分支回归 + 新手引导实现（见《15》）+ 二周目逻辑（playCount 驱动摘桃解锁） | 09/10 | 可发布版本 |

---

## 3. 每个会话固定追加的规范块（可复制）

> 继续遵守项目初始化时设定的引擎规范。本次只实现本会话列出的功能，不重构 main.lua 与 config/network 目录（本项目为单机模式，若项目无 network 目录则忽略此条），不创建 dist/ 相关文件。完成后先自查：单文件行数、枚举使用、资源路径、规则分离、防御性检查，再回复我结果与下一步建议。

---

## 4. 数据结构约定（供 TTM 参考）

> ⚠️ §1 初始化提示词单独粘贴时请一并粘贴本数据结构。

```lua
PlayerData = {
  belief = { reunion = 0, release = 0, legend = 0 }, -- 信念值
  blossoms = { wood = false, fire = false, earth = false, metal = false, water = false }, -- 五行桃花
  memories = {},      -- 已解锁记忆片段
  journal = {},       -- 旅人札记条目
  ending = "",        -- "reunion" / "release" / "legend"
  endingsSeen = {},   -- 结局收集（标题 0/3）
  playCount = 0,      -- 通关次数
}
```

> 存档读取时逐字段做类型检查，缺失字段用默认值兜底，防老存档崩溃。

### 结局判定逻辑（供 TTM 实现）

> 终局为两个行动（献花于无涕桃 / 摘下无涕桃），通向三结局。

- **① 献花于无涕桃时**：
  - 重逢值 ≥ 成全值 → S7-圆满（重逢结局，ending = "reunion"）；
  - 成全值 > 重逢值 → S8（放手结局，ending = "release"）；
  - 平局归重逢。
- **② 摘下无涕桃条件**：传说值 ≥ 3 或 playCount ≥ 1（二周目）→ S9（传说结局，ending = "legend"）。
- **③ 传说值 ≥ 3 但玩家选择献花**：正常进 S7/S8，守桃老人追加元叙事结语："这故事，你讲出了自己的版本"。
- **④ 终局开场**：回放玩家此前的关键选择瞬间（递水 / 陪坐 / 唤名）。

---

## 5. Token 与流程注意

- 活动积分只在绑定项目内使用；默认初始 3,000 Token 用于创建项目，活动绑定后优先消耗 10 万活动积分。
- 视频/音乐生成分批进行（先 P0：S1/S2/S4/S5/S7 五段，见《10-素材清单》），不要一次会话内生成全部。
- AI 积分总预算见《07-商业化与运营方案》§8，素材按 P0/P1/P2 分批消耗。
- 大功能拆多会话执行；每个会话结束时让 TTM 复盘代码并列出待办，防止"改 A 坏 B"。
- 素材生成后按《10-素材清单》验收再接入。

---

## 6. 需要向 TTM 确认的能力问题（开工后第一件事）

> 预研线索（2026-08-14，详见《19-TapTap制造官方文档研读笔记》）：字体 API（tap.loadFont）、竖屏方向 API（tap.setDeviceOrientation）、截图 API（Canvas.toTempFilePath）在 TapTap 生态均有公开文档，但制造编辑器（UrhoX+Lua）内是否可用需实测；视频接入方式与音频格式无公开结论，必须实测。

1. 视频播放的接入方式：视频资源 / UI 视频组件 / 场景视频纹理？支持哪些格式与码率？
2. 是否支持上传自定义字体（书法体/楷体）？生僻字渲染是否有坑？
3. 结局分享卡如何生成并保存到本地/分享？（是否支持截图 API，参考 Canvas.toTempFilePath 思路）
4. 音效/音乐支持的音频格式。
5. 如何锁定竖屏方向（9:16）？新建项目默认窗口方向是什么？（参考 tap.setDeviceOrientation 思路，实测制造内是否可用）
