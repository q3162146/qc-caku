# 发 TTM：S6 断点 Hook 缺陷修复

> 用法：整段复制发给 TTM。断点 Hook 真机测试发现「播到结尾卡死 + 交互未锁」缺陷，请修复后重新构建 + 出码。

══════════════════════════════════════════════════════
【S6 断点 Hook —— 真机缺陷修复（请修 + 重新构建出码）】

背景：断点 Hook（F7/「断点」按钮，`DEBUG_BREAKPOINT_TEST`，at=4.0 三选）真机回归发现**不是通过**，有一处缺陷。请先修，再重新构建 + 出真机二维码给我复测。

## 缺陷现象（真机日志）
1. **断点命中正常**：`到达断点 #1 | at=4.068 | 已暂停并等待交互`，`写入 mediaPos node=DEBUG_BREAKPOINT_TEST video=S1 breakpoint=1 timeSec=…`。
2. **但视频播到结尾后卡死**：`onPlay | phase=PAUSED_AT_BREAKPOINT current=9.969→…→10.37` 与 `onPause` **每 ~2s 反复循环**，`current` 一路到 **10.37（超过 dur=10.0）**，**从未 `ENDED`、从未 `Destroy 剧情播放器 | 存活实例=0`**。
3. **三选交互没有锁定**：`断点交互选择已锁定` **从未出现**，无信念增量日志。

## 请修复（两处）
1. **播完必须 ENDED → 释放 → 存活=0**：视频到结尾（`current ≥ dur` 或 `onEnded`）应立即走 `ENDED → 退订 → 显式 Destroy | 存活实例=0`，**不能在 `PAUSED_AT_BREAKPOINT` 状态反复 onPlay/onPause**。检查：交互选择后 `Play()` 时是否因视频已近结尾、`onEnded`/`ENDED` 分支未正确触发；或 `PAUSED_AT_BREAKPOINT` 下再次调用 `Play/Pause` 造成的边缘死循环（建议：在 ENDED/超 dur 时强制结束并释放）。
2. **三选交互必须「锁定 + 记信念」**：断点处显示三选后，用户选中应立即**锁定选项**（防连点双加）、**记录信念增量**，并打印 `断点交互选择已锁定`（和信念结果），然后 `Play()` 继续。现在这段没有日志，说明选择/锁定分支没走到或没打日志。

## 修复后
- 重新**构建（LSP 0 error）+ 出真机二维码**给我。
- 期望日志：`到达断点 #1` → `断点交互选择已锁定`（+ 信念）→ 继续 → `ENDED` → `Destroy 剧情播放器 | 存活实例=0` → `Hook 结束 | 存活实例=0`；**不应再有**结尾 onPlay/onPause 反复循环。

## 回传
修复说明 + 新构建二维码。
══════════════════════════════════════════════════════
