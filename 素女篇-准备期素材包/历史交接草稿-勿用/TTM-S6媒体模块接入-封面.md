# 发 TTM：S6 正式视频媒体接入（封面 + 实现需求）

> 用法：把「封面」段 + 《TTM-S6媒体模块接入粘贴块.md》里的 `═══` 内容一起发给 TTM。

══════════════════════════════════════════════════════
【S6 正式视频媒体接入 —— 请实现】

背景：视频技术验证（Spike）已在 PC WASM + 真机跑通并验证了 `Video.VideoPlayer` 生命周期 / Seek 精度 / 断点恢复 / 同屏 3 实例 / 三步状态机。现在进入 **S6 正式媒体接入**：把《桃素洛无幽·素女篇》的剧情视频（P01=S1 等）真正播放出来，播完推进流程。

请按下面《粘贴块》实现，**务必执行**：

## 依据
- 《21-互动影游双案例应用清单》§5 断点表（S1~S9）、§6.1/§6.2/§6.3/§6.4 恢复策略与资源管理、§7 验收清单。
- 视频 Spike 真机实测结论（本次已定案）。

## 请你做的事
1. **新建 `scripts/media/MediaPlayer.lua`**：状态机（CREATING→READY→SEEK确认→PLAY→断点Pause+写mediaPos+交互→继续→ENDED释放→推进）+ 读档恢复三步 + 冻结自愈 + 同屏≤2 + 显式销毁。
2. **改** `scripts/flow/FlowController.lua` 的 `video` 分支（现自动跳过）为 `MediaPlayer.Play(p, data_)`；`scripts/main.lua` 每帧 `MediaPlayer.Update(dt)` + 离开/切换场景 `MediaPlayer.Stop(true)`。
3. **资产映射**：`videoId("S1")→资源路径`；⚠️ 目前只有测试视频 `video/短视频生命周期 spike（推荐）/S1_test_*.mp4`，**没有正式 S1~S13** —— 先用测试视频占位映射（注明来源差异），或告知正式素材路径。
4. **构建**（LSP 0 error）+ **真机 + PC WASM 各回归一次**，按《21》§7 验收。

## 🔴 三个必须注意（来自真机实测）
- **真机 `Seek()` 不把时间拉回**（PC 正常）：断点/读档 seek 要**重试 ≤3**，反复失败则**降级**为「自然播放到目标」或「从当前位置继续」，并**记录差异**。
- **真机虚拟手势（长按底部→最近任务/切走）不触发 `AppDidEnterBackground/AppDidEnterForeground`**：后台/前台**别依赖该事件**，改用**冻结自愈**——`GetCurrentTime()` 空窗 >1.5s 无推进 → 记录 `mediaPos.timeSec` → `Pause(); Play()` 自愈 ≤3 次 → 时间恢复后续播；生命周期事件仅作辅助日志。
- **同屏 ≤2**（剧情视频 1 + 循环背景 ≤1）；剧情视频**播完即释放**（S6 连续 5 段段间必须回收，防无声/旧场景串台）；离开视频场景显式 `Destroy()`。

## 回传要求
实现 + 构建 + 真机/PC 各回归后，把结果（`MediaPlayer` 接入日志、P01 视频播放→P02、断点/读档/前后台各一条记录、有无 Lua error）发回，我据此回填《TTM-视频Spike结果汇报.md》与《21》未确认清单。
══════════════════════════════════════════════════════
